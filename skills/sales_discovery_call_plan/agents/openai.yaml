interface:
  display_name: Sales Discovery Call Plan
  short_description: Prepare a discovery call that surfaces pain, urgency, fit, and
    buying constraints.
policy:
  allow_implicit_invocation: false
