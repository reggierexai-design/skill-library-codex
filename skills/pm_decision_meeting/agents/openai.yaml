interface:
  display_name: Pm Decision Meeting
  short_description: Prepare a meeting so it actually resolves a decision instead
    of circling the issue again.
policy:
  allow_implicit_invocation: false
