interface:
  display_name: Doc Release Notes
  short_description: Turn raw changes into clear release notes grouped by user impact,
    risk, and next actions.
policy:
  allow_implicit_invocation: false
