interface:
  display_name: Ops Postmortem Author
  short_description: Write a blameless postmortem with impact, timeline, causes, and
    durable follow-up actions.
policy:
  allow_implicit_invocation: false
