interface:
  display_name: Att Landing Page Story
  short_description: Structure a landing page so attention converts into understanding,
    trust, and action.
policy:
  allow_implicit_invocation: false
